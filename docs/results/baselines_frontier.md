
<!-- generated by backend/scripts/run_baselines.py — do not hand-edit -->

**Tata Power BRSR FY24** — 40 cached pages

| system | claims | fact recall | grounding precision | gold composite | gold match |
|---|--:|--:|--:|--:|--:|
| R  rule-based (no LLM) | 455 | -- | -- | n/a *(match 6%)* | 6.0% |
| A0 llama, no repair | 319 | 20.0% | 73.4% | n/a *(match 2%)* | 2.0% |
| A1 llama + repair | 319 | 20.0% | 73.4% | n/a *(match 2%)* | 2.0% |
| S  shipped (full-doc) ⚠︎ | 339 | 54.0% | 89.2% | 96.1 | 86.0% |
| B0 frontier, no repair | 451 | 66.8% | 99.3% | n/a | 0.0% |
| B1 frontier + repair | 451 | 66.8% | 99.3% | n/a | 0.0% |

| Δ(repair) | fact recall | grounding |
|---|--:|--:|
| llama-3.3-70b | +0.0pp | +0.0pp |
| frontier | +0.0pp | +0.0pp |
| **interaction** | +0.0pp | +0.0pp |

> These two lanes read only a claim's value and page, so they observe the deterministic layer **solely where it adds, removes or rewrites a claim**. Aspect, type and unit corrections cannot appear here. A zero is therefore not evidence that the layer does nothing — only that it does nothing these lanes can see, and the gold lane that would see it is not comparable across models.

> ⚠︎ marks a row from a **different code path** (full-document run, text and tables). It is shown for context and must never be differenced against the matched A/B rows.

> Fact recall and grounding precision use mechanical, system-independent denominators and are comparable across rows. The gold composite is **not** — the gold set was sampled from the shipped extractor's output, so another system is scored only where its source sentences coincide. Where that match is under 50% the score is withheld as `n/a`: it would be computed on a handful of claims and reads as a real number.

**Shell SR2022** — 5 cached pages

| system | claims | fact recall | grounding precision | gold composite | gold match |
|---|--:|--:|--:|--:|--:|
| R  rule-based (no LLM) | 213 | -- | -- | n/a *(match 26%)* | 26.0% |
| A0 llama, no repair | 102 | 28.3% | 100.0% | n/a | 0.0% |
| A1 llama + repair | 102 | 28.3% | 100.0% | n/a | 0.0% |
| S  shipped (full-doc) ⚠︎ | 247 | 9.2% | 65.0% | 89.7 | 100.0% |
| B0 frontier, no repair | 191 | 89.7% | 100.0% | n/a | 0.0% |
| B1 frontier + repair | 191 | 89.7% | 100.0% | n/a | 0.0% |

| Δ(repair) | fact recall | grounding |
|---|--:|--:|
| llama-3.3-70b | +0.0pp | +0.0pp |
| frontier | +0.0pp | +0.0pp |
| **interaction** | +0.0pp | +0.0pp |

> These two lanes read only a claim's value and page, so they observe the deterministic layer **solely where it adds, removes or rewrites a claim**. Aspect, type and unit corrections cannot appear here. A zero is therefore not evidence that the layer does nothing — only that it does nothing these lanes can see, and the gold lane that would see it is not comparable across models.

> ⚠︎ marks a row from a **different code path** (full-document run, text and tables). It is shown for context and must never be differenced against the matched A/B rows.

> Fact recall and grounding precision use mechanical, system-independent denominators and are comparable across rows. The gold composite is **not** — the gold set was sampled from the shipped extractor's output, so another system is scored only where its source sentences coincide. Where that match is under 50% the score is withheld as `n/a`: it would be computed on a handful of claims and reads as a real number.
