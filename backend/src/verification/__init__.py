"""
ESGenuine — Satellite Evidence v2 (verification package).

Scope locked 2026-07-11 (TODO.md Phase 3; VeriGreen->CellPass split): geocode ->
Sentinel-2 L2A before/after composites -> NDVI delta + z-score -> deterministic
verdict + SHA-256 evidence bundle. No ML fine-tuning, no chain, no credentials.
"""
